
# AOR 5357-23 Unit 102 v1 — PWA (Build v13: Camera button + Obs-only)

- **Add another photo** button under each photo opens the **camera** (capture=environment) and appends a new card.
- Auto-Obs now targets **only the 'Obs' column** (never WT). Multiple matches get ticked and highlighted.
- Badge shows: "Build v13 — … (Camera+Obs-only)".

Upload all files, enable Pages, and refresh twice (or re-add to Home Screen).
